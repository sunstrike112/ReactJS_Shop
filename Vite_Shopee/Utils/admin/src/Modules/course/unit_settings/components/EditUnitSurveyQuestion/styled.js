import styled from 'styled-components'

export const Wrapper = styled.div`
	.dynamic-wrapper {
		position: relative;
		.dynamic-button {
			position: absolute;
			bottom: 25px;
			right: 10px;
			.add {
				margin-right: .2rem;
			}
		}
	}
	.back-button {
		margin-bottom: 1rem;
	}
`
